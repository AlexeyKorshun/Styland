name: Test extension
on: pull_request
jobs:
  build:
    name: Exec
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
      - run: npm install
      - run: echo $GITHUB_REF
      - run: cat package.json
      - run: npm run exec
